<h1 class="display-4 text-primary">Cards#index</h1>
<p>※ 現在、登録されているデータの一覧です</p>
<table class="table">
  <tr>
    <th>ID</th><th>題名</th>
      <th>著者</th><th colspan="2"></th>
  </tr>
  <% @cards.each do |card| %>
  <tr>
    <td width="35px"><%= card.id %></td>
    <td width="50%"><a href="/cards/<%= card.id %>">
      <%= card.title %></a></td>
    <td><%= card.author %></td>
    <td width="80px">
      <a href="/cards/edit/<%= card.id %>">編集</a></td>
    <td width="80px">
      <%= link_to "削除", "cards/#{card.id}", method: :delete, data: { confirm: "削除しますか？"} %></td>
  </tr>
  <% end %>
</table>
<p><a href="/cards/add">
    ※ 新しいデータを入力 &gt;&gt;</a></p>
<script>
function delData(id,title) {
  if (confirm('「' + title + '」のデータを削除しますか？')) {
    document.location = "/cards/delete/" + id;
  }
</script>